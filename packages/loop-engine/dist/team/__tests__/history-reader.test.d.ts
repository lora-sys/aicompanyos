/**
 * HistoryReader — 单元测试
 *
 * 覆盖：
 * - 空数据返回空前缀
 * - 有经验/能力/限制时构建完整前缀
 * - 相关性筛选（关键词匹配）
 * - 能力排序（熟练度优先）
 * - 用户画像提取
 * - 配置限制（maxExperiences 等）
 */
export {};
//# sourceMappingURL=history-reader.test.d.ts.map