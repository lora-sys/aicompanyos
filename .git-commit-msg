fix(loop-engine): integrate HistoryReader into E2E pipeline for Memory read-back loop

Root cause: src/index.ts re-export was missing HistoryReader and
DEFAULT_HISTORY_READER_CONFIG, so dist/index.js never exported the class.
Dynamic import in E2E test got `HistoryReader is not a constructor`.

Fix:
- Add HistoryReader + DEFAULT_HISTORY_READER_CONFIG to src/index.ts re-export
- Add Step 3.5 to e2e-content-production.ts: EvolutionDocsManager →
  HistoryReader.buildPromptPrefix() → enhancedWriterPrompt (prefix + original)
- E2E validated across 2 content types sharing same Memory:
  R1 newsletter: 95 score, prompt 2716→4837 chars (+78%)
  R2 seed (reuse): 85 score, prompt 1731→3852 chars (+122%)

Moat status: L1(write-only) → L2(Prompt injection active)

Teaching: Lesson 0003 (Memory Moat) + Learning Record 0003
